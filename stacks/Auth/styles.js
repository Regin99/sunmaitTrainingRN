import {StyleSheet} from 'react-native';
import {SIZES} from '../../constants/styleConstans';

const styles = StyleSheet.create({
  headerTitle: {
    fontWeight: 'bold',
    fontSize: SIZES.l,
  },
});

export default styles;
